import { PagePlaceholder } from '@/components/layout/PagePlaceholder'

export default function ScrcpyPage() {
  return (
    <PagePlaceholder
      title="Scrcpy Hub"
      description="Screen mirroring, recording, screenshot, and clipboard sync."
    />
  )
}
